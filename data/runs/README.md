# Run records

Pi writes local execution records here. Each run has a dated directory, a `manifest.json`, and Markdown artifacts. The directory is ignored by Git because run records may contain privileged legal material.

