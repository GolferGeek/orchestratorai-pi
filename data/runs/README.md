# Legacy run records

The first version wrote one directory per run here (`manifest.json`, `final.md`, `checkpoints/`). Runs now live in `data/orchestrator.sqlite`; this directory is kept only so older records remain browsable and stays git-ignored because they may contain privileged material.
