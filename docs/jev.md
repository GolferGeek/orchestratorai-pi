# Jev (TypeSafe AI)

Moved to the sibling repo **orchestratorai-jev** (`../orchestratorai-jev`), which holds the docs mirror,
the rubric library, the MCP server, and the labeled-example harness. This project consumes it through
`@orchestratorai/jev-core` in `.pi/extensions/orchestrator/` (Pi has no MCP by design).
